#include "vm/swap.h"
#include "devices/block.h"

// block_sector_t next_sector = 0;

void swap_in_frame(struct frame* frame) {
    
    // struct block* swap = block_get_role(BLOCK_SWAP);
    // block_write(swap, next_sector++, frame->frame);
}